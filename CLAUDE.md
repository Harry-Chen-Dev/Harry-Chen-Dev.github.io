# Harry-Chen-Dev.github.io

个人站点，纯静态。同时是 Shotsort 的 App Store **技术支持网址**和**隐私政策网址**。

## 不可协商的约束

- **纯 HTML + 一个 CSS 文件，没有构建步骤。** 不引入框架、打包器、Jekyll 主题、npm 依赖。改完 push 就上线是这个站点唯一的优点，别弄没了。
- **不加任何第三方脚本。** 没有统计、没有字体以外的 CDN、没有评论组件。隐私政策页写着「Shotsort 不含任何第三方」，站点自己挂个 Google Analytics 就成了自打脸。唯一的外部资源是 Google Fonts。
- **支持页和隐私页是审核材料，不是装饰。** 改动前先想清楚：
  - 支持页必须能加载、必须指明是给哪个 App 的、必须有一个真实可用的联系方式、必须在手机上好读（指南 1.5）。
  - 隐私页必须写清收集什么数据、是否给第三方、保留多久、用户怎么撤回同意和删除数据（指南 5.1.1(i)）。
- **隐私政策的每一句必须和 App 的实际行为一致。** App 一旦增加联网功能、内购、崩溃上报，这一页要先改。写在这里但 App 没做到，比没写还糟。

## 设计系统

`assets/style.css` 顶部的 `:root` 是唯一的真源，明暗双主题。

| Token | 亮色 | 用途 |
|---|---|---|
| paper | `#F3F3F0` | 页面底 |
| ink | `#171A17` | 正文 |
| ink-2 | `#4E524C` | 次要正文、导语 |
| ink-3 | `#858A82` | 元信息、页脚 |
| line | `#E2E2DC` | 分隔线 |
| accent | `#1F5A3D` | 链接、联系区左边线 |

字体：Newsreader（标题，衬线）+ 系统无衬线（正文）+ JetBrains Mono（元信息、导航、规格块）。

版式规矩：单栏，正文列宽 680px；**不用卡片**，条目之间用发丝线分隔；强调色只用在链接和联系区；正文 17px / 1.65。

## 加一个新 App 的页面

复制 `shotsort/` 整个目录改名，三个页面里的 App 名、描述、FAQ、隐私细节全部重写（**不要**只改标题就留着 Shotsort 的 FAQ），然后在 `index.html` 的 `.catalogue` 里加一个 `.entry`。

## 语言

站点是英文的 —— 它服务的是美区 App Store 的用户和审核员。仓库里的说明文档（README、这个文件）是中文。
